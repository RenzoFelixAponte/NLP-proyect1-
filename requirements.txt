# Dependencias del Proyecto 1 (NLP): fine-tuning y comparacion BERT / DistilBERT.
#
# PyTorch NO se instala desde aqui: la rueda depende de la version de CUDA de
# la maquina y pip no la resuelve solo. Instalarlo antes, por separado:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu128   # GPU
#   pip install torch                                                      # CPU
#
# Las versiones son minimos, no anclajes exactos: el proyecto corrio con
# torch 2.5.1 / transformers 4.44 en una RTX A6000.

transformers>=4.44
datasets>=2.20
scikit-learn>=1.3
matplotlib>=3.8
numpy>=1.26

# --- Solo para regenerar figuras y tablas -----------------------------------
# src/plots.py y src/tables.py leen los JSON de results/metrics/ y no necesitan
# ni GPU ni torch. Para ese caso basta con:
#
#   pip install matplotlib numpy
